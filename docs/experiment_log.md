# 실험 기록

## 2026-08-12 — Official place baseline v2 입력 구조 감사

### 내용

- 125개 Feature를 실제 DuckDB 타입·결측·registry 정의와 대조했다.
- 완전 동일 컬럼, 정의상 복원 관계, recent window 구성과 sectional 조건 혼합을 확인했다.
- target이나 모델 성능을 사용하지 않고 모델링 역할을 분류했다.

### 결과

- `MODEL_INPUT` 117, `EXCLUDE_STRUCTURAL` 6, `REVIEW_REQUIRED` 2, `AUDIT_ONLY` 0.
- finish/win/top3 count 복원과 세 history flag 관계 위반은 0건이었다.
- 현재 표본에서 동일한 sectional·마체중 count는 향후 결측 시 달라지는 비구조적 중복이었다.
- recent3/5/10 form은 유지하고 절대 race-time median 두 개만 조건 혼합 위험으로 보류했다.

### 해석

- official baseline은 Feature family를 폭넓게 유지하되 정의상 중복만 제거하는 117개 입력으로
  다음 모델 계약을 설계할 수 있다.
- 모델 학습 전 race-time 조건 통제와 결측 indicator 정책을 확정해야 한다.

## 2026-08-12 — Place Feature Snapshot v2 candidate 생성·감사

### 내용

- 49,386개 API4 semantic 원천에서 경마장·거리별 race time/S1F/G3F/G1F를 프로파일했다.
- Registry 오류를 수정하고 125개 고유 Feature Snapshot을 생성했다.
- Feature별 결측·0, 값 범위, PIT, count/rate/flag 관계와 정확히 동일한 컬럼을 감사했다.

### 결과

- Snapshot 48,524행, 4,582경주, PLC 양성 13,740행, 빌드 약 192초.
- 서울 정상 완주 sectional 27,915/27,995, 부산경남 20,473/20,473 가용.
- PIT·업무키·모집단·금지 사후정보·구조 관계 오류 0건.
- 표본상 일부 companion count가 동일했지만 정의상 중복이 아니므로 유지했다.

### 해석

- Snapshot 생성 계약 관점의 blocker는 없다.
- 모델 성능 선택 없이 official baseline 설계 단계로 넘길 수 있다.

## 2026-08-12 — KRA 세 API 최소 장애 진단

### 실험 내용

- retry 0인 클라이언트로 API4, API156, API155를 최대 1회씩 호출하도록 진단기를 작성했다.
- API4 2022 서울 1행, API156 최신 1행, API155 서울 20220108 1행을 대상으로 했다.

### 결과

- API4는 1회 응답 후 초기 parser가 문자열 `body`를 처리하지 못해 요약 전 중단됐다. 재호출하지 않았다.
- API156: HTTP 403, JSON, 0.071초, application result/total/data 없음.
- API155: HTTP 403, JSON, 0.026초, application result/total/data 없음.
- filesystem 및 Manifest의 실제 키 노출 0건.

### 해석과 다음 아이디어

- 비교 API 403은 활용 권한 부재 가능성을 먼저 확인해야 하므로 공통 KRA 장애 증거로 쓰지 않는다.
- 보완된 parser로 다음 API4 상태 확인을 1회 수행하고 정상화된 경우에만 전체 수집을 검토한다.

### 활용신청 후 API4 재확인

- 공식 데이터셋 `15058305`의 `API4_3/raceResult_3` 및 요청변수를 코드와 대조했다.
- 2022 서울 1행을 retry 없이 한 번 호출했다.
- HTTP 200, JSON, 5.203초, `resultCode=99`, `100/100`, totalCount 없음, item 0이었다.
- 키 원문·prepared URL 검사를 통해 double encoding이 없음을 확인했다.
- 호출방식 오류보다 API4 application 계층 가용성 문제라는 기존 진단을 유지한다.

### 비교 API 활용신청 후 재호출

- API156: HTTP 200, 5.369초, `resultCode=99`, `100/100`, 데이터 없음.
- API155: HTTP 200, 5.195초, `resultCode=99`, `100/100`, 데이터 없음.
- 활용신청 전의 HTTP 403이 해소되어 인증·권한 반영은 확인했다.
- API4와 같은 application 오류이므로 세 API 공통 계층 문제 가능성을 지지하는 Case B로 갱신했다.

## 2026-08-12 — Prediction cutoff 및 미확정 Feature 검증

### 실험 내용

- KRA 출전표·출전마체중·경주로 현황·경주성적 용어해설과 공공데이터 API 명세를 교차 확인했다.
- 저장된 API4 49,386행에서 부가상금 값 구조와 서울/부경 G3F·G1F 변환 관계를 점검했다.
- API25_1 소수 실호출을 시도했으나 HTTP 403으로 응답값 대조는 완료하지 못했다.

### 결과와 해석

- 12개 `NEEDS_VALIDATION` 후보를 8개 `APPROVED`, 4개 `APPROVED_WITH_FLAG`로 승격했다.
- 부경 20,473개 유효행에서 `rcTime - 누적기록 = 3F-G/1F-G`가 부동소수 오차 범위에서
  전부 일치했고, 서울의 동일 차분은 거리별로 부경 직접기록과 같은 범위였다.
- Feature 의미와 historical PIT 사용에는 blocker가 없다. 운영 시점의 API25/API4/홈페이지
  행 단위 일치 검사는 첫 성공 live batch의 후속 검증으로 남긴다.

### 다음 실험 아이디어

- Snapshot v2 생성 전 현재값에 `observed_at < prediction_cutoff` 감사 규칙을 설계한다.

## 2026-08-11 — API4 89개 필드와 Feature universe 전수 재설계

### 실험 내용

- 공식 명세 89개 필드와 Staging 49,386행을 전수 대조했다.
- 시간·마체중·주로·sectional을 새 semantic View로 정규화했다.
- Feature 후보를 target 관계 없이 정의하고 경마장·거리·연도별 원천 가용성을 프로파일링했다.

### 결과

- 공식 metadata와 Raw 필드 89/89 일치, semantic/Staging 행 수 49,386/49,386, 키 중복 0건.
- `rcTime` 소수 정밀도 불일치 0건, 완전 마체중 패턴 파싱 실패 0건.
- 마체중 자체 48,991행, 증감 45,566행 파싱 가능.
- Feature registry 141개: 승인 27, 플래그부 승인 87, 검증 필요 12, 후속 5, 금지 10.

### 해석

- API4는 첫 최소 Snapshot보다 훨씬 넓은 PIT-safe Historical 원천을 제공한다.
- G3F/G1F는 경마장별 공식 명칭이 달라 공통 의미로 아직 합칠 수 없다.
- 현재 환경·마체중은 Raw 존재만으로 예측시점 가용성을 보장하지 않는다.

### 다음 실험 아이디어

- registry 검토 후 별도 v2 Snapshot을 구현하고 성능 평가 전에 PIT·lineage·결측을 audit한다.

분석·프로파일링·비교 실험의 질문, 결과, 해석과 후속 아이디어를 날짜순으로 기록한다.
파이프라인 구현 자체는 `progress.md`, 설계 결정은 `decision_log.md`에서 관리한다.

## 2026-08-11 — API4 상태 재확인과 Raw·Feature 계층 대조

### 실험 내용

- 2022년 서울 API4 1페이지·1행 요청을 한 번만 실행했다.
- 공식 명세, Staging 49,386행, Canonical과 29개 Snapshot·28개 모델 입력을 대조했다.
- 속도·구간·마체중·날씨·주로 필드의 실제 값 가용성과 계층별 보존 여부를 확인했다.

### 결과

- 단일 요청은 다시 `resultCode=99`, 세션 100/100 오류였고 추가 요청은 하지 않았다.
- Staging에는 89개 원천 필드가 보존되지만 구간기록은 Canonical과 Snapshot에 없다.
- `wgHr`는 원문 49,386행 모두 존재하지만 Canonical `horse_weight`는 전 행 NULL이다.
- `race_time INTEGER`는 원문 소수 `rcTime`과 43,521행이 달랐다.
- 기본 서울·부산경남 S1F/G3F/G1F 계열은 대상 경마장 행의 약 97~99%에서 비0값이 있었다.

### 해석

- API 장애가 계속되어 전체 수집 재개 조건을 충족하지 못했다.
- 후속 속도 Feature는 Canonical의 현재 숫자 컬럼이 아니라 Staging 원문을 별도로 정규화해야 한다.
- 구간별 구조적 0과 결과 상태를 처리하고 과거 경주일 미만으로 집계하면 Historical PIT 후보로 사용할 수 있다.

### 다음 실험 아이디어

- API 회복 후 2022·2023 Raw 수집·audit을 먼저 완료한다.
- 별도 후속 버전에서 경마장별 기록 매핑과 `wgHr` 파서를 검증한다.

## 2026-08-11 — API4 과거 Raw 전체 수집 운영 시도

### 실험 내용

- 2022년 API4_3 서울·부산경남 전체 페이지 수집을 실행했다.
- 최초 실패 후 20초와 45초 간격의 최소 표본 요청으로 API 상태를 재확인했다.
- 실패 batch의 Raw/Manifest audit과 Staging 미적재 여부를 확인했다.

### 결과

- batch `20260811T133854446046Z_api4_3_d706b8ae`: 요청 2, 성공 0, 실패 2, `COMPLETED_WITH_ERRORS`
- 모든 확인 요청: HTTP 200, API `resultCode=99`, 세션 100/100 오류
- 실패 Raw audit 문제 0건, Staging 2022·2023 행 수 각 0건
- 2023 수집은 외부 장애가 지속되어 시작하지 않았다.

### 해석

- 사전 검증된 과거 데이터 가용성/schema 문제가 아니라 실행 시점의 외부 API 세션 포화다.
- audit 문제 0건은 실패 증거의 무결성을 뜻하며 전체 수집 완료를 뜻하지 않는다.

### 다음 실험 아이디어

- API 회복 확인 후 실패 batch를 보존한 채 새 batch로 2022·2023을 순차 수집하고 연도별 Raw audit을 완료한다.

## 2026-08-01 — API4_3과 API179_1 Raw 범위 및 결합 프로파일링

### 실험 내용

- 동일한 과거 기간과 서울·부산경남 범위에서 경주결과와 매출 API를 수집했다.
- 업무키 중복, 필수값, 날짜 범위, 경주 결합률과 승식 구성을 검사했다.

### 결과

- 경주결과: 49,386행, 4,600경주
- 매출·확정배당: 32,074행, 4,582경주
- 매출이 존재하는 모든 경주는 7개 승식 행을 보유했다.
- 경주결과에는 있지만 매출이 없는 경주는 18건이었다.

### 해석

- 경주결과와 매출은 별도 Grain의 데이터셋으로 유지해야 한다.
- 매출 누락은 곧바로 0원으로 해석할 수 없으며 상태와 완전성을 확인해야 한다.

### 다음 실험 아이디어

- 경주 상태와 매출 누락 사유를 결합해 시장 분석 적격 조건을 정의한다.

## 2026-08-02 — 비표준 `ord`와 경주 상태 검토

### 실험 내용

- `ord` 0, 91, 92, 93, 94, 95, 99의 분포와 `rcTime` 조합을 확인했다.
- 참고 저장소 정책, 한국마사회 조회 결과와 실제 경주 사례를 대조했다.

### 결과

- 93·94·95와 `rcTime=0` 조합은 DNS 규칙으로 사용할 수 있었다.
- `ord=99` 11행은 한 경주에 집중되어 있었고 다량 낙마로 인한 경주 취소로 확인됐다.
- 2025-12-26 부산경남 6경주는 전체 취소로 분류했다.
- 2026-07-06의 9경주는 결과 미확정으로 분류했다.

### 해석

- 단일 `ord` 코드보다 경주 단위 예외가 우선해야 한다.
- 취소·주행중지·실격을 하나의 일반 상태로 합치면 후속 분석에서 의미가 손실된다.

### 다음 실험 아이디어

- 새로운 비표준 코드 또는 예외 경주가 추가될 때 자동으로 검토 목록을 생성한다.

## 2026-08-02 — 월별 시장 안정성 분석

### 실험 내용

- 시장 적격 경주 4,582건을 월별로 집계했다.
- 총매출과 경주 수의 영향을 분리하기 위해 경주당 평균·중앙값 매출을 비교했다.

### 결과

- 관측 기간: 31개월
- 월별 경주 수: 108~180경주
- 월별 경주당 평균 매출: 25.27억~32.12억원
- 월별 경주당 평균 매출 변동계수: 약 5%

### 해석

- 월별 총매출은 경주 수 영향을 받지만 경주당 매출 변동은 비교적 제한적이다.
- 특정 월을 일괄 제외할 근거는 부족하며 월은 추세·이상치 확인 차원으로 유지한다.

### 다음 실험 아이디어

- 연도별 동일 월 비교와 휴장·특별경주 일정이 월별 차이를 설명하는지 확인한다.

## 2026-08-02 — 서울과 부산경남 시장 구조 비교

### 실험 내용

- 경주 수, 매출 비중, 경주당 평균·중앙값 매출과 승식별 매출 비중을 비교했다.

### 결과

- 서울: 2,698경주, 경주 비중 58.9%, 매출 비중 62.5%
- 부산경남: 1,884경주, 경주 비중 41.1%, 매출 비중 37.5%
- 서울의 경주당 평균 매출은 부산경남보다 약 16% 높았다.
- 두 지역의 승식별 매출 비중 차이는 최대 0.93%p였다.

### 해석

- 지역 차이는 통제해야 하지만 시장 구성이 단절된 별도 모집단으로 보기는 어렵다.
- 두 경마장을 모두 포함하고 경마장 차원으로 비교하는 것이 적절하다.

### 다음 실험 아이디어

- 등급 구성 차이를 통제한 후에도 경마장별 경주당 매출 차이가 유지되는지 확인한다.

## 2026-08-02 — 등급과 승식별 시장 구성 분석

### 실험 내용

- 10개 등급과 7개 승식의 경주 수, 총매출 비중과 경주당 매출을 비교했다.

### 결과

- 국5·국6등급이 시장 적격 경주의 약 52.9%를 차지했다.
- 등급별 경주당 평균 매출은 21.95억~40.08억원이었다.
- 승식별 매출 비중은 삼복승식 34.2%, 복승식 31.5%, 삼쌍승식 17.6% 순이었다.
- 연승식 매출 비중은 1.7%였다.

### 해석

- 경주 수가 많은 등급과 경주당 매출이 높은 등급이 일치하지 않으므로 전체 등급을
  유지해야 한다.
- 매출은 승식의 시장 규모만 나타내며 모델 적용 가능성이나 수익성을 입증하지 않는다.
- 매출만으로 연승식·복승식 또는 다른 승식을 확정할 수 없다.

### 다음 실험 아이디어

- `confirmed_odds_raw`를 적중 조합·배당 단위로 정규화한다.
- 승식별 적중배당 분포, 극단값 의존도와 조합 난도를 비교한다.

## 2026-08-02 — 공식 적중배당 원문 프로파일링

### 실험 내용

- 시장 적격 4,582경주의 7개 승식 `confirmed_odds_raw` 32,074행을 검사했다.
- 원문을 공백 2개로 분리하고 마번 조합, 선택 수와 양수 배당을 검증했다.
- 승식별 원문 길이, 행별 조합 수, 배당 분위수, 중복 마번과 중복 조합을 검사했다.

### 결과

- 빈 원문은 0건이었다.
- 32,074개 행에서 50,494개 공식 적중 조합을 추출했고 파싱 오류는 0건이었다.
- 행별 조합 수는 단승·복승·쌍승·삼복승·삼쌍승 1~2개, 연승 2~4개, 복연승 3~6개였다.
- 마번 16 표기 `(16)`을 포함한 조합은 25개였다.
- 중복 마번, 중복 canonical 조합과 0 이하 배당은 모두 0건이었다.
- 중앙값 배당은 연승식 1.9에서 삼쌍승식 181.9까지 큰 차이가 있었다.

### 해석

- 현재 데이터 형식은 결정론적으로 파싱 가능하지만 한 원본 행에 여러 적중 조합이 존재한다.
- 정규화 Grain은 `경주 × 승식 × 공식 적중 조합`이어야 한다.
- 배당 분포 차이만으로 수익성이나 모델 적합성을 판단할 수 없다.

### 다음 실험 아이디어

- 공식 적중배당 Canonical 테이블을 구현하고 원본 행·조합 수를 대사한다.
- 승식별 극단값 의존도와 적중 조합의 구조를 경주 조건별로 비교한다.

## 2026-08-02 — 동착과 승식별 적중 조합 대사

### 실험 내용

- 복수 적중 조합이 있는 경주를 공식 1~3위와 대조했다.
- 공동 1·2·3위 경주 수와 단승·복승·쌍승·복연승·삼복승·삼쌍승 조합 수를 비교했다.
- 연승식 2개·3개 조합 경주의 등록두수와 최종 유효 출전두수를 비교했다.

### 결과

- 공동 1위 7경주, 공동 2위 14경주, 공동 3위 7경주였다.
- 단승 2조합 7건, 복승 2조합 14건, 쌍승 2조합 21건이 공식 순위와 일치했다.
- 공동 3위 7경주에서 연승 4조합, 복연승 6조합, 삼복승 2조합이 함께 관찰됐다.
- 삼쌍승 2조합 28건은 공동 1·2·3위 7+14+7경주와 일치했다.
- 8두 등록 후 제외마가 발생한 경주에서는 연승 2조합과 3조합이 모두 관찰됐다.

### 해석

- 복수 조합은 데이터 중복이 아니라 동착과 공식 승식 정산 결과다.
- 연승식은 제외 시점에 따른 발매 기준을 현재 경주결과 API만으로 복원할 수 없다.
- 공식 배당 원문을 정산의 최종 근거로 보존해야 한다.

### 다음 실험 아이디어

- 승식별 배당 분포와 조합 난도를 결합해 후보 승식을 비교한다.

## 2026-08-02 — 승식 후보 파레토 비교

### 실험 내용

- 유효 출전마 수로 승식별 경주 단위 가능 조합 수를 계산했다.
- 공식 적중 조합 수를 가능 조합 수로 나눠 나이브 적중확률을 계산했다.
- 매출 비중, 나이브 적중확률, 선택 공간과 배당 꼬리비율로 파레토 지배 관계를 비교했다.
- 등록두수 기준과 동착 28경주 제외 결과를 민감도 분석했다.

### 결과

- 비지배 후보는 연승식·복승식·복연승식·삼복승식이었다.
- 단승식은 연승식, 쌍승식은 복승식에 지배됐다.
- 삼쌍승식은 복승식 또는 삼복승식에 지배됐다.
- 유효 출전마와 등록두수가 다른 605경주에서도 집계 기준을 바꾸면 파레토 집합은 동일했다.
- 동착 28경주를 제외해도 파레토 집합은 동일했다.

### 해석

- 연승식은 접근성, 복승식과 삼복승식은 시장 규모, 복연승식은 중간 절충점이라는 서로 다른
  역할이 있어 네 후보를 단일 점수로 서열화할 근거가 없다.
- 확정배당 분포는 시장 구조를 설명하지만 ROI를 의미하지 않는다.

### 다음 실험 아이디어

- 첫 모델의 목적과 구현 복잡도를 기준으로 연승식·복승식을 우선 검토한다.

## 2026-08-02 — 6B 연승 모집단과 Feature 가용성 점검

실험 내용:

- 완료 상태이며 공식 연승 적중 원문이 있는 경주의 출전 결과 상태를 집계했다.
- 공식 연승 적중마와 `race_id + gate_no` 결합 무결성을 검사했다.
- 기본 컬럼 결측과 말·기수·조교사의 경주일 이전 이력 깊이를 측정했다.
- DNS가 없는 경주를 모집단 민감도 집합으로 계산했다.

결과:

- 4,582경주·49,183개 결과 행 중 `is_valid_start` 행은 48,524개였다.
- 공식 연승 양성 13,740행은 모두 출전마와 결합됐고 invalid-start 양성은 0건이었다.
- DNS는 659행·605경주였고, DNS 없는 집합은 3,977경주·42,632 valid-start 행이었다.
- 주행정지 133행과 실격 3행은 실제 출전으로 분류되어 있었다.
- 마체중은 48,524행 전부 결측이었다.
- 과거 출전이 없는 행은 말 4,973개, 기수 334개, 조교사 249개였다.

해석:

- 현재 데이터로 개발용 확률모델 모집단과 타깃은 구성할 수 있다.
- `is_valid_start`는 사후 프록시이므로 실제 발매 마감 모집단을 완전히 복원하지는 못한다.
- DNS 포함 여부에 따른 민감도 비교와 이력 관측창 불완전 플래그가 필요하다.

다음 실험 아이디어:

- 승인된 Feature Snapshot에서 전체 프록시와 DNS 없는 집합의 분포·Calibration 차이를 비교한다.
- 기준모델 이후에만 추가 정보의 증분가치를 시간순 ablation으로 평가한다.

## 2026-08-02 — DNS 605경주 모델링 정책 검증

실험 내용:

- DNS가 있는 완료·공식 연승 경주를 경주 Grain으로 집계했다.
- DNS 수, 유효 출전마, 완주·주행정지·실격, 경주 상태와 7개 공식 승식 완전성을 검사했다.
- 등록·결과 행, 1착과 공식 연승 적중마 수의 무결성을 검사했다.

결과:

- DNS 1두 555경주, 2두 46경주, 3두 4경주였다.
- 601경주(99.34%)가 1~2두 DNS였고 최대 DNS는 3두였다.
- 유효 출전마 중앙값은 10두, 최소는 6두였다.
- 605경주 모두 완료 상태이고 7개 승식, 1착과 2~4개 공식 연승 적중마가 존재했다.
- 경주 취소·결과 미확정·승식 누락·행 수 불일치·미해결 비출전 상태는 0건이었다.
- 주행정지 말이 있는 13경주도 공식 결과와 승식이 정상이며, 실격 동반 경주는 0건이었다.

해석:

- DNS는 정상 시행 경주의 개별 비출전 상태이며 DNS 자체는 경주 단위 제외 근거가 아니다.
- DNS 말만 제외하고 실제 출전마는 보존하는 정책이 데이터 손실과 선택 편향을 줄인다.

다음 실험 아이디어:

- Feature Snapshot 구현 후 DNS 경주와 DNS 없는 경주의 Feature·타깃 분포 차이를 보조 점검한다.
- 최종 선택 후 해당 승식의 타깃과 Point-in-Time Feature 범위를 설계한다.

## 2026-08-08 — API26_2 과거 출전표 Point-in-Time 실제 호출 검증

### 실험 내용

- 서울·부산경남 각 1두의 2024~2025 경주일 4개씩, 총 8개 과거 날짜를 현재 시점에 호출했다.
- 통산·최근1년 출전 및 1·2·3위 횟수를 API4에서 경주일 이전 조건으로 직접 계산해 비교했다.
- 레이팅·부담중량·등록두수와 주요 경주·출전정보를 API4 해당 경주 값과 대조했다.

### 결과

- 두 말의 통산·최근1년 출전 및 1·2·3위 횟수 8개는 네 과거 날짜에서 각각 모두 같은 값이었다.
- 2024년 최초 표본의 API4 사전 출전은 0회였지만 API26 통산 출전은 41회와 82회였다.
- 레이팅·부담중량·등록두수를 포함한 사전정보 13개는 8개 표본 모두 API4와 일치했다.
- 레이팅은 27~36, 부담중량은 52.0~56.5kg, 등록두수는 9~12두로 날짜별 변화를 보였다.

### 해석

- API26 누적 성적은 과거 경주일 당시 값이 아니라 현재 또는 이후 결과가 반영된 값이다.
- API26에는 시간적으로 안전한 경주별 사전정보와 누수 위험이 있는 현재성 누적정보가 혼재한다.
- 누적 이력은 API4에서 Point-in-Time으로 직접 계산해야 한다.

### 다음 실험 아이디어

- 레이팅을 첫 Snapshot에 추가할지 현재 28개 최소 범위를 유지할지 결정한다.
- 실제 운영 전에는 미래 경기 API26을 경기 전에 수집해 공개·변경 시각을 검증한다.

## 2026-08-08 — API26 `ilsu` 의미와 속도 원천 가용성 검증

### 실험 내용

- 기존 API26 8개 표본의 `ilsu`를 직전 실제 출전일·정상 완주일 간격과 비교했다.
- API4 605개 경주일에서 날짜 내 일관성, 결과 상태별 존재 여부와 관측일 순번을 점검했다.
- 승인된 4,582경주의 valid-start 48,524행에서 경주기록·구간기록·환경·마체중·착차 원문을
  `0` 센티널과 실제값으로 나눠 프로파일링했다.
- API26 중복 조건 필드 12개를 같은 API4 행과 대조했다.

### 결과

- API26 `ilsu` 8건은 API4 같은 경주 값과 일치했지만, 비교 가능한 6건 모두 직전 출전·완주
  간격과 달랐다.
- 같은 경마장·날짜의 `ilsu`는 605/605일 단일값이었고 DNS 659행, 주행중지 134행, 실격 3행에도
  모두 존재했다.
- 경마장·연도별 관측 경주일 dense rank와는 108일이 달라 정확한 공식 번호 산식은 복원하지 않았다.
- API26의 조건·상금 관련 중복 필드 12개는 8개 표본 모두 API4와 일치했다.
- API4에 48,524행의 속도·구간·날씨·주로·마체중·착차 원문이 이미 존재했다. 반대 경마장 구간
  필드와 미통과 구간은 `0` 센티널이었다.

### 해석

- `ilsu`는 말 이력이 아니라 경주일 캘린더 속성이다.
- API4 개별 이벤트는 직접 PIT 집계할 수 있지만 API37 누적값은 요청 당일 포함으로 누수 위험이 있다.
- Canonical 마체중 결측은 원천 부재가 아니라 `중량(증감)` 문자열 파싱 공백이다.

### 다음 실험 아이디어

- 첫 기준모델 완료 뒤 동일 날짜순 분할에서 속도·구간·마체중·주로 적합성 Feature를 ablation한다.
- 속도지수 설계 시 거리·경마장·주로상태와 경주 전개 보정 방식을 먼저 비교한다.

## 2026-08-09 — `place_feature_snapshot_v1` 생성·감사

### 실험 내용

- 승인된 4,582경주의 valid-start 행을 `race_id + horse_id` Grain으로 물리화했다.
- 29개 Feature와 공식 PLC 타깃을 생성하고 모집단·키·원천·상태·NULL·PIT 규칙을 감사했다.
- 주행중지·실격을 실제 출전 분모에 유지하고 DNS를 제외하는지 단위 테스트와 전체 데이터에서 확인했다.

### 결과

- Snapshot 48,524행, 공식 연승 양성 13,740행, 말 과거 이력 없음 4,973행이었다.
- DNS 0행, 주행중지 133행, 실격 3행이었다.
- `rating`, 연령, 부담중량은 전 행에서 사용 가능했다.
- `source_max_event_date >= feature_as_of`와 전용 감사 이슈는 모두 0건이었다.

### 해석

- 승인된 모집단·상태·PIT 계약을 유지하면서 첫 모델 입력을 재현 가능한 테이블로 만들었다.
- 212행은 말·기수·조교사 어느 쪽에도 참조 가능한 과거 이벤트가 없어
  `source_max_event_date`가 NULL이며 이는 허용된 신규 이력 사례다.

### 다음 실험 아이디어

- 모델 학습 전에 날짜순 학습·검증·테스트 경계와 평가 지표를 6D에서 확정한다.

## 2026-08-09 — Feature Snapshot 월별 left-censoring 프로파일

### 실험 내용

- 월별 경주·행·PLC 양성률과 말·기수·조교사 이력 가용률을 집계했다.
- 엔터티별 prior start count의 월별 P25/P50/P75를 비교했다.
- 서로 다른 warm-up 종료점을 사용하는 날짜순 분할 후보 3개를 계산했다.

### 결과

- 월별 경주는 108~180개, 행은 1,230~1,952개, PLC 양성률은 25.88~30.51%였다.
- 기수·조교사 이력은 2024년 2~3월부터 거의 99~100% 가용했다.
- 말 이력 가용률은 2024-04에 92.46%였지만 prior count 중앙값은 2회였다.
- 2024-10부터 말 이력 가용률은 대체로 91~97%, 중앙값은 5회 이상이었다.
- 2024-09-30 warm-up 종료 후보는 Train 18,888행, Validation 4,823행, Final Test 11,179행을
  확보했다.

### 해석

- left-censoring 종료점은 관계자보다 말 이력 깊이를 기준으로 결정하는 것이 적절하다.
- 2024년 9월 말은 이력 깊이와 Train·Final Test 규모를 함께 보존하는 균형점이다.
- 계절별 신규 말 유입으로 보이는 가용률 하락은 warm-up 연장만으로 완전히 제거되지 않는다.

### 다음 실험 아이디어

- 후보 B가 공식 분할로 채택됐다. 다음 단계에서 첫 확률모델의 비교 기준과 평가 절차를 설계한다.

## 2026-08-09 — 연승 기준모델 Validation 비교

### 실험 내용

- Train 18,888행에서 무정보 기준선과 28개 입력 Logistic Regression을 적합했다.
- 과거 전체 학습→이후 3개월 예측의 세 OOF fold로 sigmoid Calibration 후보를 만들었다.
- Validation 4,823행·439경주에서 기준선·Logistic 원본·Logistic+sigmoid를 비교했다.
- 선택 후 Final Test를 읽지 않고 동일 Logistic 절차를 Train+Validation 23,711행에 재적합했다.

### 결과

- macro Log Loss: 기준선 0.589293, Logistic 원본 0.512157, sigmoid 0.513856
- macro Brier: 기준선 0.199888, Logistic 원본 0.169727, sigmoid 0.170210
- Logistic 원본이 두 지표 모두 가장 낮아 `SEALED_BEFORE_FINAL_TEST` 상태로 선택됐다.
- Final Test 예측과 평가는 생성하지 않았다.

### 해석

- 승인된 28개 Feature의 Logistic 절차는 무정보 확률보다 Validation 확률 품질이 좋았다.
- sigmoid는 원본보다 소폭 나빠 첫 최종 절차에서 제외하는 것이 적절하다.
- 이는 Validation 선택 결과이며 최종 일반화 성능 주장은 봉인된 Final Test 평가 전에는 하지 않는다.

### 다음 실험 아이디어

- 사용자 승인 후 설정을 변경하지 않고 Final Test를 한 번 평가한다.

## 2026-08-09 — 봉인된 연승 기준모델 Final Test

### 실험 내용

- 봉인된 run contract와 Train+Validation Pipeline의 SHA256을 확인했다.
- 설정 변경이나 재적합 없이 2026-01~07의 1,050경주·11,179행을 한 번 평가했다.
- Train+Validation 양성률 무정보 기준선과 Logistic 원본을 비교했다.

### 결과

- Logistic macro Log Loss 0.532267, macro Brier 0.177908
- 무정보 기준선 macro Log Loss 0.598040, macro Brier 0.203972
- Logistic micro Log Loss 0.529311, micro Brier 0.176639
- Calibration intercept 0.057017, slope 0.986839
- Validation 대비 macro Log Loss +0.020110, macro Brier +0.008181

### 해석

- Logistic은 Final Test에서도 무정보 기준선보다 우수했다.
- 전체 Calibration은 0/1 기준에 가까웠지만 5~7월로 갈수록 손실이 증가하고 7월 slope가
  0.766으로 낮아졌다.
- 이 결과는 최종 평가이며 사후 재적합이나 설정 변경 근거로 사용하지 않는다.

### 다음 실험 아이디어

- 기준모델 한계를 정리한 뒤 후속 Feature 고도화는 새 모델 버전과 독립된 평가 계획으로 설계한다.

## 2026-08-09 — 월별 expanding Walk-forward 시간 안정성

### 실험 내용

- 2024-10~2025-09를 최초 학습기간으로 사용하고 2025-10부터 한 달씩 평가했다.
- 이후 매 Fold에서 과거 학습창을 한 달 확장해 2026-07까지 총 10개 Fold를 실행했다.
- 28개 Feature, 전처리 규칙과 raw Logistic 설정을 고정하고 월별 calibration drift를 측정했다.
- 기존 baseline_v1과 Final Test 산출물의 실행 전후 SHA256을 대조했다.

### 결과

- 2025-10~2026-04 평균 macro Log Loss 0.515343, Brier 0.170911
- 2026-05~07 평균 macro Log Loss 0.551345, Brier 0.185909
- 5~7월 macro Log Loss는 0.533340→0.551044→0.569651로 연속 상승
- 모든 평가월에서 무정보 기준선 대비 Log Loss와 Brier 개선 유지
- 7월 calibration intercept -0.2520, slope 0.7683
- 모든 Fold 수렴, 예측확률 오류 0건, baseline 산출물 변경 0건

### 해석

- 5~7월 저하는 단일 월 이상치만으로 보기 어려운 연속 패턴이다.
- 기준선도 같은 기간 악화돼 모델 drift와 모집단·시장 구조 변화가 함께 있을 수 있다.
- 3개월 이동평균도 상승했지만 보조 진단일 뿐 추세 확정 규칙은 아니다.
- 시간 진단 결과를 기존 Final Test나 baseline_v1 수정에 사용하지 않는다.

### 다음 실험 아이디어

- 모델 변경 없이 2026-05~07의 입력 분포·경주조건·양성률 변화를 앞선 안정 기간과 비교한다.

## 2026-08-09 — 2026-05~07 Feature·타깃·경주 구성 drift

### 실험 내용

- 안정기간 2025-10~2026-04와 저하기간 2026-05~07을 비교했다.
- 경주 구조는 경주 Grain, 말·관계자 Feature와 타깃은 출전마 Grain으로 집계했다.
- 28개 입력의 분위수·NULL률과 범주 비중 변화를 계산했다.
- 등록두수별 안정기간 PLC 양성률을 저하기간 등록두수 mix에 적용했다.

### 결과

- 12두 경주 비중 31.62%→0.22%, 8~10두 경주 16.45%→40.22%
- PLC 양성률 27.32%→29.36%, +2.04%p
- 저하기간 mix에 안정기간 등록두수별 양성률을 적용한 기대값 29.30%
- 최근5 PLC 적중률 P75 0.50→0.40, 최근 평균착순 중앙 5.60→5.75
- 말 이력 가용률 94.25%→94.08%, 관계자 이력 가용률은 거의 100% 유지

### 해석

- 타깃 양성률 상승은 거의 전부 작은 등록두수 경주 증가와 함께 설명된다.
- 최근 폼 분포도 소폭 악화됐지만 모델 손실의 인과 원인으로 확정할 수 없다.
- 경마장·관계자 적중률·결측 변화는 주된 설명으로 보기 어렵다.
- 행 단위 walk-forward 예측이 없어 구성 고정 후 모델 손실의 잔여 변화는 이번 범위에서 미확인이다.

### 다음 실험 아이디어

- 후속 모델을 만들기 전에 기존 Final Test와 분리된 새 평가 계약을 설계한다.

## 2026-08-09 — 안정기간 경주 단위 Bootstrap 표본 변동 진단

### 실험 내용

- 안정기간 1,034경주의 walk-forward 손실을 경주 단위 모집단으로 구성했다.
- 월별 실제 경주 수를 유지하고 복원추출 10,000회, seed `20260809`로 Macro Log Loss와
  Macro Brier 기준분포를 만들었다.
- 3개월 보조 분포는 세 월의 표본 크기를 각각 유지한 월별 Macro의 동일가중 평균으로 계산했다.

### 결과

- 5월 tail: Log Loss 1.20%, Brier 0.70%
- 6월·7월 tail: 두 지표 모두 0/10,000
- 5~7월 평균 tail: 두 지표 모두 0/10,000
- 안정기간 7개 fold 재현값은 기존 Macro 지표와 절대오차 0이었다.

### 해석

- 5월도 분포 상단 꼬리지만 6~7월보다 덜 극단적이다.
- 6~7월 및 3개월 평균 저하는 안정기간 경주 표본 변동만으로 설명하기 어려울 가능성이 있다.
- 경험적 tail은 p-value가 아니며 시간 순서·월별 구성·모델 업데이트 차이를 반영하지 않는다.

### 다음 실험 아이디어

- 새 모델을 만들지 않고 `registered_runner_count`별 조건부 손실을 분해해 구성 변화와
  동일 조건 내 성능 변화의 동반 여부를 확인한다.

## 2026-08-10 — 등록두수 구성 효과와 동일 조건 손실 분해

### 실험 내용

- 안정기간과 저하기간 walk-forward 경주별 Log Loss·Brier를 등록두수별로 집계했다.
- 공통 support에서는 대칭 구성/동일구간 분해를 사용하고 비공통 희소 구간은 별도 bridge
  효과로 분리해 전체 차이에 정확히 재조정했다.

### 결과

- Log Loss: 전체 +0.034512, 구성 +0.013978, 동일 구간 +0.020334,
  비공통 support +0.000200
- Brier: 전체 +0.014377, 구성 +0.004862, 동일 구간 +0.009443,
  비공통 support +0.000071
- 동일 구간 Log Loss 기여는 11두 +0.008220, 10두 +0.004556, 12두 +0.003769,
  9두 +0.003606 순이었다.
- 10개 월 fold의 기존 Macro 지표를 절대오차 0으로 재현했다.

### 해석

- 작은 등록두수 경주 증가가 손실 상승 방향으로 작용했지만 전체 저하를 설명하지 못했다.
- 동일 등록두수 내 손실 상승이 더 큰 부분이므로 등록두수 mix만의 문제로 진단을 종료하기 어렵다.
- 이 분해는 다른 경주 조건을 통제하지 않은 기술적 분해이며 인과효과가 아니다.

### 다음 실험 아이디어

- 표본이 충분하고 동일 구간 효과가 큰 9·10·11두 경주에서 월별 손실과 확률 오차가 특정
  경마장·거리·등급에 집중되는지 제한적으로 확인한다.

## 2026-08-10 — API4 2022·2023 과거 조회·호환성 사전 검증

### 실험 내용

- API4 `rc_month`로 48개 연도·경마장·월의 존재 여부와 출전행 수를 조회했다.
- 각 연도·경마장의 1·6·12월 대표 일자 12건에서 전체 경주결과를 메모리로만 검사했다.
- 과거 표본 필드와 신뢰된 2024 Raw 필드 집합, 핵심 필드 결측을 비교했다.

### 결과

- 월별 공백 0건, 대표 일자 정상 응답 12/12
- 2022 18,355행, 2023 19,284행
- 과거·2024 모두 89개 필드, 누락·추가 0개
- 핵심 경주·결과·관계자·rating 필드 표본 결측 0%
- 대표 경주일 경주당 8~15두, 현재와 같은 등급 문자열 확인

### 해석

- 스키마와 월별 연속성은 추가 수집에 충분하다.
- 2022 공식 시행계획은 코로나 상황에 따른 탄력 운영을 명시하므로 2022를 곧바로 이후와 같은
  학습기간으로 취급하는 것은 보류한다.
- 2022 전체는 이력 warm-up, 2023년부터 장기 비교에 사용하는 구조가 품질과 이력 깊이의 균형이 좋다.

### 다음 실험 아이디어

- 2022·2023 전체 Raw 수집과 audit 후 연도·경마장별 필드·상태코드·중복·월별 경주 수를 확정한다.
## 2026-08-12 — API4 Historical extension 품질 audit

### 실험 내용

- 2022·2023 서울·부산경남 전체 API4 Raw의 페이지·행·해시·업무키·월별 연속성을 검사했다.
- audit 통과 대표 batch를 Staging에 적재하고 Semantic v2의 파싱·범위·경마장별 sectional을 비교했다.
- 2024~2026과 rating, 관계자 ID, rcTime, 마체중, S1F/G3F/G1F 가용성을 비교했다.

### 결과와 해석

- 37,639행, 3,465경주, 24개월 공백 0, Raw/Staging/Semantic 행 불일치 0이었다.
- 경주시간·마체중·sectional 숫자 파싱의 비현실 범위는 0건이었다.
- 2023 `ord=99` 98행의 `track='(%)'`는 구조적 결측으로 확인됐다.
- 주요 가용성은 기존 기간과 유사하여 2022·2023을 PIT Historical 원천으로 승계할 수 있다.

### 다음 실험 아이디어

- Snapshot v2 전체기간 재생성 후 월별 history availability와 left-censoring 완화 정도를 프로파일링한다.
## 2026-08-13 — `wgHr` 직접 차이 검증과 Semantic 회귀 audit

### 결과

- `중량()` 직전 실제 출전 비교 가능 5,430행 중 5,417행이 0kg이었다.
- 13개 반례와 명시적 `+N/-N` 불일치 200건은 모두 2026년 7월에 한정됐다.
- Semantic 갱신 후 `중량()` 5,843행은 0, `()` 663행은 NULL이었다.
- 전체 87,025행에서 파싱·범위·sectional source 위반은 0건이었다.

### 해석과 다음 실험

- 빈 증감 괄호는 0kg을 의미한다고 확정할 수 있다.
- Snapshot 확장 전 2022·2023 공식 PLC Sales 원천을 확보해 Historical PLC Feature를 같은 계약으로
  계산할 수 있는지 검증한다.
## 2026-08-13 — Sales History Extension 및 Snapshot v2 Rebuild Audit

### 실험 내용

- 2022·2023 Sales를 연도별로 전체 수집하고 Raw SHA/page/row 및 Staging lineage를 감사했다.
- 감사된 API4/Sales 대표 batch로 2022~2026 Canonical·Star·Snapshot v2를 재생성했다.
- 연도별 모델 모집단과 Historical Feature 가용성, PIT/leakage 및 구조 관계를 검사했다.

### 결과와 해석

- Sales 24,178행, Canonical 8,065경주, Snapshot 85,566행/8,036경주가 구축됐다.
- 공식 PLC payout-출전마 미결합과 핵심 구조/PIT 위반은 0건이다.
- horse history 가용성은 2022 82.07%에서 2023 93.61%로 증가해 2022 warm-up의 left-censoring 완화 효과가 확인됐다.
- 125/117 Feature 계약은 전체기간에서도 유지 가능하다.

### 다음 실험 아이디어

- 모델을 실행하기 전에 2022 warm-up과 장기 Train/Validation/walk-forward 경계를 설계하고 봉인한다.
## 2026-08-13 — 117개 Feature 월별 maturity 프로파일

### 실험 내용

- 2022-01~2023-07 월별 경주·출전마·PLC 모집단 통계와 117개 입력의 non-null 비율을 계산했다.
- Historical family는 단순 NULL 대신 prior/recent/condition companion count로 실제 관측 존재와 full recent window를 구분했다.
- 2022-07, 2023-01, 2023-07 시작 후보의 표본과 maturity를 비교했다.

### 결과와 해석

- 2022-01 horse history 7.22%, recent3 full 0%에서 2023-01 각각 93.91%, 75.11%로 개선됐다.
- 2023-01 recent5 full은 58.69%, recent10 full은 22.66%로 깊은 창은 이후에도 점진적으로 성숙했다.
- 급격한 단절점은 없지만 한 해 전체 warm-up과 장기 표본 보존을 함께 만족하는 2023-01-01이 가장 해석 가능한 시작점이다.

### 다음 실험 아이디어

- 2023-01 이후 Train/Validation 및 장기 expanding walk-forward 경계를 Feature maturity 계약과 분리해 설계한다.
## 2026-08-13 — Baseline v2 날짜 분할 및 시간순 평가 설계

### 실험 내용

- 2023-01~2026-07의 월별 경주·출전마·PLC 모집단 통계와 경마장·등록두수·등급·거리 구성을 집계했다.
- 24개월 Train+6개월 Validation, 18개월 Train+12개월 Validation, 24개월 Train+12개월 Validation의 세 후보를 비교했다.
- 월별·분기별 walk-forward 평가 표본 규모를 비교했다.

### 결과와 해석

- 권고안은 Train 28,392행, Validation 18,615행, post-selection 평가 20,428행이며 세 역할 모두 충분한 표본과 계절 커버리지를 제공한다.
- 월별 walk-forward는 31개 fold, fold당 108~180경주로 장기 안정성 진단에 적절하고 분기 rollup으로 변동성을 보조 요약할 수 있다.
- 날짜 선택에는 Log Loss·Brier·예측값을 사용하지 않았다.

### 다음 실험 아이디어

- 봉인된 날짜 역할을 사용해 baseline v2의 제한된 전처리·Logistic·calibration 후보 실행 계약을 구현한다.

## 2026-08-13 — 속도·sectional lineage 및 조건 혼합 감사

### 실험 내용

- Snapshot과 117개 입력 inventory에서 race time·S1F·G3F·G1F 계열 16개를 전수 추적했다.
- 85,566개 유효 출전행을 경마장, 거리, 경마장×거리로 나눠 count·결측률·P5/P25/median/P75/P95를 계산했다.
- 부산경남 누적 구간값과 직접값, 서울 Semantic 계산식을 전체기간 대조했다.

### 결과와 해석

- 부산경남 G3F/G1F 비교 가능 35,695건은 각각 불일치 0건·최대 오차 0초였다.
- 서울·부산경남 중앙값은 S1F 14.0/14.2초, G3F 39.4/39.5초, G1F 13.8/13.9초였다.
- 거리별 분포 차이는 있었지만 고정 길이 sectional의 물리적 의미는 유지됐다.
- 절대 race-time median 2개만 논리적 제외하고 14개 속도계 입력을 유지했다. 타깃과 모델 성능은 사용하지 않았다.

### 다음 실험 아이디어

- 확정된 117개 계약을 사용해 Train-only 전처리 및 제한된 Logistic baseline 절차를 구현한다.
- 조건 정규화 speed figure는 official baseline 이후 별도 Feature 버전에서 설계한다.

## 2026-08-17 — Official place Logistic baseline v2 Validation

### 실험 내용

- Train-only 전처리와 단일 L2 Logistic을 117개 입력에 적합했다.
- Train 내부 5개 expanding 3개월 OOF 예측으로 sigmoid를 적합했다.
- Validation에서 무정보 기준선, raw Logistic, sigmoid 세 후보만 평가했다.

### 결과와 해석

- 무정보/raw/sigmoid macro Log Loss는 0.599945/0.540603/0.540252였다.
- macro Brier는 0.204862/0.180464/0.180511로 sigmoid가 raw보다 소폭 높았다.
- sigmoid 개선이 일관되지 않아 raw Logistic을 봉인하고 Train+Validation 47,007행에 재적합했다.
- OOF 시간 순서·전처리 fit 범위 위반과 수렴 경고는 0건이었다. 모델 실행 경로는 post-selection을
  읽지 않았지만 사전 건수 확인 SQL이 해당 기간 집계 양성률을 출력한 제한사항이 있었다.

### 다음 실험 아이디어

- 봉인된 v2 artifact를 변경하지 않고 2025-07~2026-07 post-selection temporal evaluation을 한 번 실행한다.

## 2026-08-18 — 후속 성능 개선 실험 설계

### 실험 내용

- 기존 Validation 결과와 117개 Feature family만 사용해 비선형 모델 및 Feature engineering 가설을 정리했다.
- 2023-01~2024-06 내부 4개 quarterly expanding fold와 제한된 후보 예산을 설계했다.
- 모델 복잡도, 상대 경주시간, pace-shape와 조건부 결합 효과를 분리하는 ablation 순서를 정의했다.

### 결과와 해석

- 첫 모델 후보는 같은 117개를 사용하는 Histogram Gradient Boosting으로 제한했다.
- 첫 Feature 후보는 PIT-safe Historical 상대 경주시간과 pace-shape 두 bundle로 제한했다.
- 2025-07~2026-07은 모든 후보 봉인 뒤 한 번 비교하는 공통 temporal evaluation으로 보존한다.
- 새 데이터 조회·모델 학습·prediction·Feature 구현은 수행하지 않았다.

### 다음 실험 아이디어

- 2024-07 이후 접근을 거부하는 개발 loader, 4-fold inner temporal CV와 실험 registry를 먼저 구현한다.

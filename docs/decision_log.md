# 의사결정 기록

## 2026-08-12 — Official place baseline v2 입력 후보 117개

- Snapshot 125개 중 현재 사전정보 28개와 Historical 정보 89개를 모델 입력 후보로 둔다.
- `count × rate`로 복원되는 장기 finish/win/top3 count 3개와 `count > 0`으로 복원되는
  history flag 3개는 구조 제외한다.
- 표본에서 우연히 같은 sectional·마체중 count는 정의상 중복이 아니므로 유지한다.
- 여러 거리·경마장을 섞은 recent3/5 절대 race-time median 2개는 모델 입력에서 보류한다.
- count는 rate·average·sectional의 신뢰도와 경험량을 나타내므로 companion 입력으로 유지한다.
- ID·날짜·lineage·타깃·모집단 상태는 관리·감사용으로만 사용한다.

## 2026-08-12 — Official place Snapshot v2 candidate 계약

- Registry 행 수가 아니라 고유 Feature 이름을 구현 계약으로 사용한다.
- 중복 5개를 제거하고 G3F/G1F 관측 count 4개를 추가해 registry 140개,
  즉시 구현 대상 125개로 확정했다.
- 서울은 `rcTime - 누적기록`, 부산경남은 직접 `F-G` 기록으로 최종 G3F/G1F를
  공통화한다. S1F도 양 경마장의 첫 구간 초로 공통화한다.
- 경마장 전용 중간구간·코너 기록은 의미와 거리 구조가 달라 공통화하지 않는다.
- 현재 사전정보의 historical timestamp 부재는 limitation으로 기록하되 기존 cutoff
  검증에 따라 날씨·주로·함수율·마체중을 candidate에 포함한다.
- 표본에서 동일한 companion count는 미래 결측 시 달라질 수 있으므로 성능 검토 없이
  제거하지 않는다.

## 2026-08-12 — API 예외는 공통 redaction 경로만 사용

- API 예외 객체를 `str(error)`로 직접 로그·Manifest에 저장하지 않는다.
- query parameter 이름의 대소문자와 URL encoding 여부에 관계없이 ServiceKey 값을
  `***REDACTED***`로 치환한다.
- 현재 키는 변경하지 않으며 공공데이터포털 활용신청 상태와 별개로 관리한다.
- API4가 정상 application response를 반환하기 전에는 2022·2023 전체 수집을 재개하지 않는다.

이유: HTTP client 예외는 전체 URL을 포함할 수 있으며, 비교 API의 HTTP 403은 API별 권한 문제와
공통 backend 문제를 구분하지 못하므로 최소 관측 이상의 결론을 내릴 수 없다.

## 2026-08-12 — Official place baseline prediction cutoff와 Feature 판정

- prediction cutoff는 경주 결과 전의 실제 베팅 의사결정 시점으로 정의하고 정확한 `T-N분`은
  운영 설계 전까지 고정하지 않는다.
- 실시간 배당은 baseline Feature가 아니라 betting-stage 정보로 분리한다. API4 확정 배당은
  사후정보이므로 계속 금지한다.
- 현재 마체중·증감·날씨·주로상태·함수율 및 부가상금 1~3은 경주 전 이용 가능한 Feature로 승인한다.
- 현재 경주의 sectional은 금지하되, 과거 G3F/G1F는 경마장별 원천 정의를 최종 600m/200m
  기록으로 정규화하는 조건으로 승인한다.
- 서울 G3F/G1F는 `rcTime - seG3fAccTime`, `rcTime - seG1fAccTime`; 부경은
  `bu_3fGTime`, `bu_1fGTime`을 사용한다.
- 기존 baseline_v1은 historical experiment로 보존하며 재평가하지 않는다.

이유: KRA 사전 공개 화면과 공식 API 명세가 현재 경주 정보의 사전 이용 가능성을 보여주고,
부경 누적/구간 원천 20,473행의 정확한 항등관계와 서울 변환값의 거리별 분포가 sectional의
동일한 물리 의미를 뒷받침한다.

## 2026-08-11 — `place_logistic_baseline_v1`을 historical experiment로 전환

- 기존 코드·계약·Validation·Final Test·walk-forward·진단 산출물은 수정·삭제하지 않는다.
- 향후 공식 기준모델로는 사용하지 않고 초기 최소 Feature 파이프라인과 평가 절차를 검증한 historical experiment로 보존한다.
- 이유는 성능 실패가 아니라 API4 Raw 재점검에서 속도·구간·마체중·경주조건 정보가 기존 Feature 설계에 충분히 반영되지 않았기 때문이다.
- 새 공식 baseline은 별도 semantic·Snapshot·모델 버전과 독립된 평가 계약으로 만든다.

## 2026-08-11 — API4 정규화는 `semantic.api4_runner_event_v2`로 분리

- Raw·Staging과 기존 Canonical 의미는 변경하지 않는다.
- `rcTime`은 소수 초를 보존하고, `wgHr`는 중량·증감, `track`은 상태·함수율로 새 semantic column에 분리한다.
- S1F는 경마장별 원천을 공통 의미로 매핑한다.
- G3F/G1F는 서울 누적기록과 부산경남 구간기록의 의미 동등성이 확정되지 않아 공통 컬럼으로 합치지 않는다.
- 모든 결과·구간값은 현재 경주 Feature로 금지하고 과거 경주일 미만 집계에만 사용한다.

중요한 설계 변경과 범위 결정을 날짜순으로 기록한다. 새로운 결정을 추가할 때 기존 기록을
삭제하거나 현재 결론에 맞춰 소급 수정하지 않는다. 결정이 바뀌면 새 항목으로 남긴다.

## 2026-08-01 — 기존 저장소를 읽기 전용 참고자료로 사용

결정:

- `horse_racing`은 정책과 과거 구현을 확인하는 읽기 전용 자료로만 사용한다.
- 생성·수정·삭제·Commit·Push는 `kra-racing-analytics`에서만 수행한다.

이유:

- 기존 R 프로젝트의 재현성과 이력을 보존하면서 Python + SQL 기반의 독립적인 프로젝트를
  구축하기 위해서다.

## 2026-08-01 — Python 환경은 기존 Anaconda의 별도 Conda 환경 사용

결정:

- 기존 Anaconda 설치를 사용하되 프로젝트 전용 `kra-racing-analytics` 환경을 만든다.
- VS Code는 필수 도구로 채택하지 않는다.
- JupyterLab은 탐색과 분석에 사용한다.

이유:

- base 환경 오염을 막고 사용자가 익숙한 Anaconda 환경에서 재현 가능성을 유지하기 위해서다.

## 2026-08-01 — 초기 데이터베이스로 DuckDB 채택

결정:

- 초기 SQL 저장소는 로컬 파일 기반 DuckDB를 사용한다.
- SQL Server나 외부 서버형 DB는 현재 범위에 포함하지 않는다.

이유:

- 별도 서버 운영과 라이선스 비용 없이 Python·Jupyter·SQL 분석을 빠르게 시작할 수 있고,
  향후 Star Schema와 Power BI 연결에 필요한 SQL 구조를 학습할 수 있기 때문이다.

## 2026-08-01 — Raw 증거를 먼저 보존하고 Staging에 적재

결정:

- API 응답 원문은 불변 파일로 보존한다.
- request_id, batch_id, SHA256, 수집시각을 Manifest에 기록한 뒤 SQL Staging에 적재한다.
- Canonical 검사는 Staging 적재 후 별도 테이블을 생성하는 방식으로 수행한다.

이유:

- 원문 추적, 재처리, 변환 버전 비교와 향후 증분·자동 운영 확장을 가능하게 하기 위해서다.

## 2026-08-01 — 초기 운영은 수동 전체 재구축

결정:

- 최신 증분 수집과 자동 운영은 후속 단계로 미룬다.
- 초기에는 과거 범위 전체를 수동으로 재구축한다.

이유:

- 데이터 계약과 상태 정책을 먼저 안정화하고 초기 프로젝트 복잡도를 제한하기 위해서다.

## 2026-08-02 — 경주 단위 상태가 출전마 상태보다 우선

결정:

- 2024-09-29 서울 2경주와 2025-12-26 부산경남 6경주는 `RACE_CANCELLED`로 관리한다.
- 2026-07-06의 확인된 9경주는 `RESULT_NOT_FINALIZED`로 관리한다.
- 경주 취소·결과 미확정 상태는 해당 경주의 모든 출전 결과에 우선 적용한다.

이유:

- `ord` 단일 값만으로 서로 다른 경주 수준의 상황을 확정할 수 없으며, 경주 전체 상태가
  공식 순위의 유효성을 결정하기 때문이다.

## 2026-08-02 — 취소 사유는 분석 상태에서 분리

결정:

- 분석 상태는 `RACE_CANCELLED`로 통일한다.
- 다량 낙마 같은 구체적인 사유는 `evidence_note`에만 보존한다.
- Canonical 경주 본문에는 `status_reason`을 두지 않는다.

이유:

- 취소 사유를 모든 경주에서 일관되게 수집할 수 없고, 사유별 상태값 증가가 분석을
  불필요하게 복잡하게 만들기 때문이다.

## 2026-08-02 — 시장 분석 모집단은 4,582경주

결정:

- 정상 완료되고 공식 7개 승식 매출이 모두 있는 경주만 시장 분석 Mart에 포함한다.
- 취소·결과 미확정·매출 누락 경주는 Fact에 보존하고 Mart에서만 제외한다.
- 매출 없는 경주를 0원으로 대체하지 않는다.

이유:

- 완전한 승식 간 비교를 보장하면서도 제외된 원천과 이유를 추적하기 위해서다.

## 2026-08-02 — 모든 등급과 승식을 보존한 뒤 분석으로 범위 결정

결정:

- 현재 관측된 10개 등급을 모두 분석한다.
- 7개 승식을 모두 Star Schema와 시장 분석에 포함한다.
- 연승식·복승식은 관심 후보지만 분석 전에 모델 후보로 확정하지 않는다.

이유:

- 사전 선택으로 시장 구조를 왜곡하지 않고 월·경마장·등급·승식 비교 결과를 근거로 후속
  범위를 정하기 위해서다.

## 2026-08-02 — DBeaver를 공식 분석 실행 경로로 사용하지 않음

결정:

- 분석과 검증은 프로젝트의 DuckDB SQL, Python과 Jupyter Notebook으로 수행한다.
- DBeaver는 현재 프로젝트 흐름에서 사용하지 않는다.

이유:

- 자동 실행, Git 버전 관리와 재현성이 DBeaver의 수동 화면 작업보다 중요하기 때문이다.

## 2026-08-02 — 시각화 도구는 최종 보고서 단계에서 확정

결정:

- 현재 탐색 분석은 Jupyter와 Matplotlib을 사용한다.
- Power BI 등 최종 보고서 도구는 분석 구조와 포트폴리오 목적이 확정된 뒤 결정한다.

이유:

- 데이터 모델과 분석 질문을 먼저 안정화하고 시각화 도구에 종속되는 조기 결정을 피하기
  위해서다.

## 2026-08-02 — 공식 적중배당은 조합 단위로 정규화

결정:

- 정규화 목표 Grain은 `경주 × 승식 × 공식 적중 조합`으로 한다.
- 동착으로 한 경주·승식에 여러 공식 조합이 있어도 모두 별도 행으로 보존한다.
- 쌍승식·삼쌍승식은 마번 순서를 보존하고, 복승식·복연승식·삼복승식은 정렬된 canonical
  조합을 함께 저장한다.
- `confirmed_odds_raw`, 원문 내 순서와 lineage를 보존한다.
- 확정배당은 모델 Feature가 아니라 시장 분석과 백테스트 정산에만 사용한다.

이유:

- 32,074개 원본 행에서 50,494개 적중 조합이 관찰됐고 동착 시 통상 단일 조합 승식에도
  2개 조합이 존재해 `경주 × 승식` Grain만으로는 정보가 손실되기 때문이다.

## 2026-08-02 — 연승식 적중 범위는 공식 배당 원문을 우선

결정:

- 연승식은 공식 원문에 기록된 1·2위 또는 1·2·3위 적중 조합을 그대로 보존한다.
- 경주결과 API의 등록두수나 최종 유효 출전두수만으로 적중 범위를 다시 계산하지 않는다.
- 감사에서는 연승 적중마가 공식 순위의 1·2위 또는 1·2·3위와 일치하는지만 확인한다.

이유:

- 8두 등록 후 제외마가 발생한 경주에서 연승 적중마 2두와 3두가 모두 관찰돼, 현재 데이터만으로
  발매 시점의 출주두수 기준을 확정할 수 없기 때문이다.

## 2026-08-02 — 승식 후보는 종합점수 대신 파레토 집합으로 관리

결정:

- 승식별 경주 수는 후보 비교 지표에서 제외한다.
- 매출 비중·나이브 적중확률은 높을수록, 가능 조합 수·배당 꼬리비율은 낮을수록 유리한
  파레토 축으로 사용한다.
- 연승식·복승식·복연승식·삼복승식을 비지배 후보로 유지한다.
- 단승식·쌍승식·삼쌍승식은 1차 후보에서 제외한다.
- 임의 가중치와 종합점수는 사용하지 않는다.

이유:

- 각 승식은 시장 규모와 예측 난도 사이의 서로 다른 절충점을 가지며, 가중치를 임의로 정하면
  사용자의 목적을 데이터가 대신 결정한 것처럼 보일 수 있기 때문이다.

## 2026-08-02 — 첫 모델은 연승식, 복승 기본안은 조합 직접 분류

결정:

- 첫 완성 모델은 연승식 적중 여부를 타깃으로 하는 `경주 × 출전마` 확률모델로 한다.
- 첫 모델의 목적은 Feature 파이프라인, 날짜순 검증, 확률평가와 Calibration 구조 완성이다.
- 복승식은 핵심 후속 목표로 유지하고 첫 구현은 비순서 2두 조합 직접 분류를 기본안으로 한다.
- 별도 결합모델과 순위 기반 공동확률 모델은 복승 기준모델 이후 비교 후보로 남긴다.
- 연승 모델 출력이 복승 조합확률로 자동 전환된다고 가정하지 않는다.

이유:

- 연승식은 첫 확률모델의 데이터·검증 구조를 완성하기에 단순하고 양성 표본이 상대적으로
  충분하다.
- 말별 연승 주변확률만으로 두 말의 복승 공동확률은 식별되지 않으므로 복승에는 별도의 조합
  확률모델이 필요하다.

## 2026-08-02 — 6A는 데이터 계약 원칙, Feature 목록은 6B에서 설계

결정:

- 6A에서는 Feature의 분류 원칙과 추가 API 필요성 판단 원칙만 정의한다.
- 실제 Feature 목록은 6B에서 설계한다.
- 실제 추가 API 목록과 수집 여부는 6B 이후 Feature 가용성 검토 결과를 바탕으로 결정한다.

이유:

- 데이터 계약과 구체적인 Feature 설계를 분리해 예측시점·누수 방지 원칙을 먼저 고정하면서도,
  현재 데이터의 가용성을 확인하기 전에 불필요한 API 수집 범위를 확정하지 않기 위해서다.

## 2026-08-02 — 연승 모델링 데이터 계약 확정

결정:

- 예측 Grain은 `경주 × 베팅 가능 출전마`, 타깃은 공식 PLC 적중마 여부로 한다.
- 운영 예측시점은 연승 발매 마감 직전으로 두며, 일 단위 이력은 경주일보다 이전 자료만 쓴다.
- 학습 경주는 완료 상태, 공식 PLC 적중 결과, 베팅 가능 모집단 복원 조건을 모두 만족해야 한다.
- 공식 PLC 원문으로 타깃을 생성하고 등록두수로 적중 범위를 다시 계산하지 않는다.
- Train·Validation·Final Test는 경주일 기준 연속 구간으로 분할하며 같은 경주를 나누지 않는다.
- Log Loss, Brier Score와 Calibration을 주 평가로 사용한다.
- 현재 `is_valid_start`를 발매 마감 당시 베팅 가능 여부와 동일하다고 간주하지 않는다.

이유:

- 공식 연승 결과를 보존하면서 사후 상태가 Feature나 예측 모집단에 유입되는 누수를 막기
  위해서다.
- 모델의 첫 목적이 베팅 수익 최적화가 아니라 재사용 가능한 확률모델 검증 구조의 완성이므로
  확률의 정확성과 보정 상태를 우선 평가한다.

## 2026-08-02 — 6B 모집단은 개발 프록시와 민감도 집합으로 구분

결정:

- 완료·공식 연승 경주의 `is_valid_start = true` 행을 첫 모델의 개발용 프록시 모집단으로
  제안한다.
- DNS가 없는 경주를 별도 모집단 민감도 집합으로 관리한다.
- 현재 프록시의 결과를 발매 마감 시점의 실제 운영 모집단 성능으로 표현하지 않는다.
- 부족한 정보는 계약 필수·기준모델 우선·성능 고도화·있으면 좋음의 네 단계로 판단한다.
- 추가 API는 현재 데이터 기준모델과 가용성 검토 없이 선제적으로 수집하지 않는다.

이유:

- 공식 타깃은 완전하게 결합되지만 DNS가 있는 605경주에서는 현재 결과 데이터만으로 발매 마감
  당시의 베팅 가능 여부를 입증할 수 없기 때문이다.
- 첫 모델의 목적은 운영 배치가 아니라 데이터·시간순 검증·확률평가 구조의 완성이므로 불확실성을
  명시한 프록시와 민감도 비교가 최소 구현에 적합하다.

## 2026-08-02 — DNS는 말 단위 제외, 정상 경주는 유지

결정:

- `result_status='DNS'`인 말은 모델링 행에서 제외하고 음성 타깃으로 만들지 않는다.
- DNS가 있다는 이유만으로 경주 전체를 제외하지 않는다.
- `RACE_STOPPED`, `DISQUALIFIED`는 실제 출전 행으로 유지한다.
- 경주 단위 제외는 취소·결과 미확정·공식 연승 누락·타깃 결합 실패·미해결 비출전 상태 등
  경주 전체의 학습 계약이 깨지는 경우로 제한한다.
- DNS 없는 3,977경주는 주 모집단 대안이 아니라 선택적 민감도 집합으로 유지한다.

이유:

- DNS 605경주 중 601경주(99.34%)는 1~2두 DNS였고 3두 DNS 4경주도 유효 출전마 8두로
  정상 진행됐다.
- 605경주 모두 완료 상태·7개 승식·1착·공식 연승 결과가 정상이며 경주 단위 이상은 0건이었다.
- 경주 전체를 제외하면 정상 출전마 5,892행을 불필요하게 잃고 선택 편향을 만들기 때문이다.

## 2026-08-08 — 첫 역사적 Feature Snapshot은 API4_3 최소 Feature로 구성

결정:

- 추가 API 없이 API4_3만으로 첫 역사적 Feature Snapshot을 만든다.
- 현재 기본정보 8개와 최소 Point-in-Time 이력 Feature 20개를 첫 모델 입력으로 사용한다.
- 실제 출전수에는 정상 완주·주행중지·실격을 포함하고 DNS는 제외한다.
- PLC 적중률은 공식 PLC 적중수/실제 출전수로 계산한다. 주행중지·실격은 실패로 포함하되
  착순 평균에서는 제외한다.
- 같은 날짜 결과는 전부 제외하고 `source_max_event_date < race_date`를 강제한다.
- Snapshot 구현 전 명세를 먼저 확정하며 이번 단계에서는 구현하지 않는다.

이유:

- 첫 모델의 목적은 Feature 파이프라인, 시간순 검증과 Calibration의 기준선을 완성하는 것이다.
- 완주마만 비율 분모로 사용하면 실제 출전 후 비완주를 제거하는 선택 편향이 생긴다.
- DNS를 실패로 포함하면 출전 가능성과 연승 적중확률을 혼합하게 된다.
- 과도한 파생 Feature를 피하고 기준모델 이후 ablation으로 확장하기 위해서다.

## 2026-08-08 — API26 누적 성적은 금지하고 경주별 사전정보만 PIT 후보로 유지

결정:

- API26의 통산·최근1년 출전 및 1·2·3위 횟수 8개는 Historical Feature로 사용하지 않는다.
- 레이팅·부담중량·등록두수와 경주·출전마·관계자 기본정보는 과거 경주별 PIT 후보로 인정한다.
- `ilsu`는 현재 `days_since_last_start`와 의미가 일치하지 않아 보류한다.
- API4에 동일한 사전정보가 있으므로 첫 Snapshot을 위한 API26 대량 과거 수집은 하지 않는다.
- 레이팅을 현재 28개 Feature에 추가할지는 별도 결정으로 남긴다.

이유:

- 두 말의 2024~2025 네 날짜에서 누적 8개 필드가 각각 하나의 값으로 고정됐고 최초 경주에도
  이후 결과가 반영되어 있었다.
- 사전정보 13개는 API4의 해당 과거 경주 값과 8개 표본 모두 일치하고 날짜별 변화도 확인됐다.

## 2026-08-08 — `ilsu`는 Feature에서 제외하고 속도 이력은 API4 개별 이벤트로 계산

결정:

- API26 `ilsu`는 말의 휴식일수가 아닌 경마장·경주일 단위 속성이므로 첫 Snapshot에서 제외한다.
- API37의 최고·최저·평균 구간기록은 요청 경주일을 포함하므로 모델 Feature 원천으로 사용하지 않는다.
- 향후 속도·페이스 Feature는 API4의 개별 과거 경기 기록에서 `race_date < feature_as_of`로 직접
  계산한다.
- 속도·구간·마체중·착차·주로 적합성은 첫 기준모델 이후 고도화로 미룬다.
- `rating`은 첫 Snapshot 즉시 추가 권장 후보로 두되 기존 28개 명세 변경은 사용자 승인 뒤 한다.

이유:

- `ilsu`는 605개 경주일 모두 같은 경마장·날짜 안에서 단일값이고 실제 출전 간격과 일치하지 않았다.
- 현재 API4에 valid-start 48,524행의 개별 기록과 조건 원문이 있어 누적 API를 사용할 이유가 없다.
- 거리·경마장·주로·전개를 보정하지 않은 경주시간은 능력 외 요인이 섞이므로 기준모델에 즉시
  추가하기보다 별도 설계와 ablation이 필요하다.

## 2026-08-08 — 첫 Feature Snapshot을 `rating` 포함 29개로 확정

결정:

- API4 staging의 `rating`을 현재 경주 기본 Feature에 추가한다.
- 첫 모델 입력은 현재 기본정보 9개, 말 이력 14개, 기수 이력 3개, 조교사 이력 3개로 총 29개다.
- `ilsu`는 Feature 후보에서 제외한다.
- 속도·구간기록·주로·마체중은 기준모델 이후 고도화 후보로 유지한다.
- API37 누적 구간기록은 사용하지 않고, 향후 API4 개별 과거 기록을 PIT 조건으로 직접 집계한다.
- 마체중은 원천 부재가 아니라 결합 문자열 파싱 공백으로 기록한다.
- 이번 결정에서는 명세만 변경하고 Snapshot은 구현하지 않는다.

이유:

- `rating`은 과거 API26 표본과 API4가 8/8 일치한 경주별 사전정보이며 추가 수집 없이 사용할 수 있다.
- `ilsu`는 말의 휴식일수와 무관하고, API37은 요청 경주일을 포함하는 누적값이다.
- API4에는 향후 PIT-safe 속도·페이스 Feature를 직접 계산할 개별 과거 기록이 이미 존재한다.

## 2026-08-09 — 첫 Feature Snapshot을 버전된 `mart` 테이블로 물리화

결정:

- 첫 Snapshot을 `mart.feature_snapshot_place`에 `place_feature_snapshot_v1`로 저장한다.
- 실행 결과와 버전은 `mart.feature_snapshot_run`에서 관리한다.
- 생성은 트랜잭션으로 수행하고 전용 감사가 실패하면 rollback한다.
- Snapshot에는 승인된 29개 Feature만 포함하고 속도·구간 계열은 추가하지 않는다.
- 모델 학습은 별도 6D 설계 승인 전까지 수행하지 않는다.

이유:

- 버전·PIT 경계·원천 배치·정책·타깃을 한 행에서 추적할 수 있어 재현성과 누수 감사를 유지한다.
- 기존 Canonical과 Star를 재사용하면서 모델 입력 Grain을 독립적으로 고정할 수 있다.

## 2026-08-09 — 6D 날짜 분할은 후보 B로 확정

결정:

- Warm-up은 2024-01-05~2024-09-30으로 둔다.
- Train은 2024-10-01~2025-09-30, Validation은 2025-10-01~2025-12-31,
  Final Test는 2026-01-01~2026-07-26으로 고정한다.
- Warm-up 경주는 후속 경주의 Point-in-Time Feature 이력 계산에 사용하지만 모델 학습,
  모델 선택, Calibration과 성능 평가 표본에서는 제외한다.
- 이번 결정은 날짜 분할 정책만 확정하며 모델 학습, 전처리기 적합과 Calibration은 수행하지 않는다.

이유:

- 2024년 10월부터 말 prior start count 중앙값이 5회 이상으로 올라 초기 left-censoring 영향이
  충분히 완화된다.
- 기수·조교사 이력은 그보다 이른 2024년 2~3월부터 거의 99~100% 가용하므로 말 이력 깊이가
  warm-up 종료점을 결정하는 제약이다.
- 12개월·18,888행의 Train과 7개월·11,179행의 Final Test를 유지해 이력 깊이와 학습·평가
  표본 규모의 균형을 확보한다.

## 2026-08-09 — 첫 기준모델은 28개 입력의 Logistic Regression으로 설계

결정:

- Snapshot Feature 29개는 유지하되 `horse_prior_plc_hit_count`는 관리·감사용으로만 사용한다.
- `horse_prior_start_count`와 `horse_prior_plc_hit_rate`를 포함한 나머지 28개를 첫 모델에 입력한다.
- Train 양성률을 모든 행에 부여하는 무정보 기준선과 고정 설정 L2 Logistic Regression을 비교한다.
- 결측 중앙값, 범주 사전과 표준화 통계는 Train에서만 학습하고 Pipeline으로 모델과 결합한다.
- sigmoid Calibration은 Train 내부 날짜순 expanding-window OOF 확률에만 적합한다.
- Validation macro Log Loss를 주 선택 기준, macro Brier와 Calibration 진단을 보조 기준으로 한다.
- Final Test는 전처리·모델·Calibration 선택을 마친 뒤 한 번만 평가한다.

이유:

- PLC 적중 횟수는 출전 횟수에 종속되고 출전수와 적중률이 이미 입력에 있어 기준모델에서 중복성이
  크지만, 산식 감사에는 유용하므로 Snapshot에서 보존할 가치가 있다.
- Logistic Regression과 단일 sigmoid Calibration은 첫 확률 파이프라인을 검증하는 데 충분하며
  불필요한 모델·튜닝 범위 확장을 피한다.
- Train 전용 전처리와 시간순 OOF Calibration은 Validation·Final Test 정보가 학습 과정으로
  유입되는 것을 막는다.

## 2026-08-09 — Final Test 전 Train+Validation 재적합을 확정

결정:

- Validation에서 최종 절차를 선택하고 모든 설정을 봉인한 뒤 Train+Validation 전체로 재적합한다.
- Logistic Regression은 동일한 28개 입력, 전처리 규칙과 하이퍼파라미터를 그대로 사용한다.
- sigmoid Calibration이 선택되면 Train calibrator를 재사용하지 않는다.
- Train+Validation에서 `과거 전체로 학습하고 이후 3개월을 OOF 예측`하는 expanding-window
  원칙으로 calibration 학습용 확률을 다시 생성하고 calibrator를 재적합한다.
- 모든 재적합을 끝낸 뒤 Final Test를 단 한 번 평가한다.

이유:

- Validation까지의 과거 데이터를 최종 적합에 활용하면서도 모델·전처리·Calibration 선택과
  Final Test 평가를 분리할 수 있다.
- 특정 날짜를 규칙으로 고정하는 것보다 3개월 예측창의 시간순 expanding-window 원칙을 고정해야
  분할 기간이 확장되더라도 같은 누수 방지 절차를 재현할 수 있다.
- calibrator도 최종 모델의 적합 범위와 대응하는 OOF 확률에서 다시 학습해야 Train 범위에서 만든
  보정 함수를 기계적으로 재사용하는 불일치를 피할 수 있다.

## 2026-08-09 — Validation에서 Logistic 원본 확률을 최종 절차로 선택

결정:

- `place_logistic_baseline_v1`의 최종 절차로 Calibration 없는 Logistic 원본 확률을 선택한다.
- 선택된 28개 입력, Train 기반 전처리 규칙과 Logistic 하이퍼파라미터를 봉인한다.
- 동일 설정으로 Train+Validation 23,711행에 최종 Pipeline을 재적합한다.
- sigmoid가 선택되지 않았으므로 Train+Validation calibration OOF와 calibrator는 생성하지 않는다.
- Final Test 예측과 평가는 별도 승인 전까지 수행하지 않는다.

이유:

- Validation macro Log Loss는 Logistic 원본 0.512157로 무정보 기준선 0.589293과 sigmoid
  0.513856보다 낮았다.
- macro Brier도 Logistic 원본 0.169727로 기준선 0.199888과 sigmoid 0.170210보다 낮았다.
- sigmoid가 두 주 지표를 개선하지 못했으므로 더 단순한 원본 확률을 유지하는 것이 확정 계약의
  선택 규칙에 맞는다.

## 2026-08-09 — 봉인된 Final Test를 1회 평가하고 결과를 동결

결정:

- 봉인된 `place_logistic_baseline_v1` contract와 Pipeline을 변경 없이 Final Test에 한 번 적용한다.
- 평가 결과를 확인한 뒤 모델·전처리·Feature·Calibration을 재적합하거나 변경하지 않는다.
- 같은 모델 버전의 Final Test 결과 파일이 존재하면 평가 명령의 재실행을 거부한다.
- 후속 고도화는 별도 모델 버전과 새 실험으로 관리한다.

이유:

- Logistic macro Log Loss 0.532267과 macro Brier 0.177908은 무정보 기준선 0.598040,
  0.203972보다 낮아 기준모델의 일반화 개선을 확인했다.
- Validation 대비 손실은 증가했으므로 Final Test를 추가 선택이나 튜닝에 사용하지 않고 결과를
  그대로 동결해야 평가 독립성을 보존할 수 있다.

## 2026-08-09 — 첫 연승 baseline_v1 완료 및 Walk-forward를 진단 전용으로 분리

결정:

- `place_logistic_baseline_v1`은 Final Test까지 완료된 첫 연승 기준모델로 확정한다.
- 월별 expanding walk-forward는 기존 모델을 수정하거나 새 최종 성능을 주장하는 절차가 아니라
  시간 안정성 진단으로만 사용한다.
- 첫 Fold는 baseline_v1과 동일한 최초 모델 학습기간 2024-10~2025-09를 사용한다.
- Feature 이력에는 기존 Warm-up 데이터가 반영된 Snapshot을 그대로 사용한다.
- 3개월 이동평균은 보조 진단이며 추세 확정이나 모델 변경 기준으로 사용하지 않는다.

이유:

- 기존 Final Test의 독립성과 완료 상태를 보존하면서 월별 손실과 calibration drift를 관찰하기
  위해서는 모델 선택과 시간 진단을 분리해야 한다.
- 고정 절차의 월별 재학습 평가를 통해 특정 월 저하와 연속적 저하를 구분할 수 있다.
## 2026-08-12 — 2022·2023은 대표 audit 통과 batch만 Staging에 적재

결정:

- 연도별로 Raw audit을 통과한 대표 batch 하나만 Staging과 Semantic v2 확장에 사용한다.
- 도구 반환시간 제한 때문에 중복 생성된 완전 Raw batch는 immutable 증거로 보존하되 적재하지 않는다.
- 2022·2023의 선택적 조교사·마주 키 생략과 상태 연계 구조적 결측은 NULL로 보존하며 schema drift로 보지 않는다.
- 기존 `semantic.api4_runner_event_v2` 정의와 Snapshot/model 보호 계약은 변경하지 않는다.

이유:

- 동일 범위 Raw를 둘 다 적재하면 업무키가 중복되므로 lineage가 명확한 대표 batch만 승계해야 한다.
- 전체 고유 89필드 구조와 핵심 가용성이 2024~2026과 호환되고, 생략은 일부 행의 선택적 필드에 한정됐다.
## 2026-08-13 — 숫자 중량 뒤 빈 괄호는 0kg, 중량 없는 괄호는 NULL

결정:

- `480()`는 `horse_weight_change_kg=0`으로 파싱한다.
- `()`는 마체중과 증감을 모두 NULL로 유지한다.
- 2026년 7월 원천 불일치는 값을 보정하지 않고 limitation으로 유지한다.
- 2022·2023 공식 PLC 적중 원천이 확보되기 전에는 착순 기반 대체 타깃을 만들지 않는다.

이유:

- 2026년 7월 이전 직전 실제 출전 비교 5,313/5,313행이 0kg으로 일치했다.
- 확정 데이터 계약은 공식 winning payout만 PLC 타깃 근거로 허용한다.
## 2026-08-13 — 감사된 연도별 batch를 다중 batch Canonical 범위로 결합

결정:

- 2022·2023·2024~2026 대표 Raw/Staging batch의 lineage를 유지한 채 하나의 Canonical 실행 범위로 결합한다.
- `winning_payout_v1`, 7승식 완전시장 모집단, 125 Feature와 117 MODEL_INPUT 정의는 변경하지 않는다.
- 2023 공식 Sales가 없는 11경주는 기존 계약에 따라 모델 모집단에서 제외하고 임의 보정하지 않는다.

이유:

- 연도별 immutable batch를 합치거나 덮어쓰지 않아도 전체기간을 재구축할 수 있고, 각 행의 source batch lineage를 보존할 수 있다.
- 공식 PLC 적중 원천과 PIT 정책을 바꾸지 않고 Historical 기간만 안전하게 확장할 수 있다.
## 2026-08-13 — official place baseline v2 모델링 시작일 2023-01-01 권고

결정:

- 2022-01~12는 Historical Feature 계산을 위한 warm-up으로 사용한다.
- official place baseline v2의 모델링 가능 기간은 2023-01-01부터 시작하는 것으로 권고한다.
- 날짜 경계는 모델 성능이 아니라 117개 입력의 월별 가용성, companion count와 표본 구조로만 결정한다.

이유:

- 2023년 1월 horse long-term·속도/sectional·마체중 이력은 93.91%, same-meet 93.83%, 기수·조교사·마주는 약 99~100% 가용했다.
- recent10과 interaction은 점진적으로 성숙해 명확한 단절점이 없으므로 임의 threshold 대신 한 해 전체 warm-up이라는 재현 가능하고 해석하기 쉬운 경계를 채택했다.
- 2023년 7월까지 늦춰도 신규 말·조합의 left-censoring은 사라지지 않으며 모델링 기간만 6개월 줄어든다.
## 2026-08-13 — Official place baseline v2 고정 분할과 walk-forward 계약

결정:

- Warm-up은 2022년, Train은 2023-01~2024-06, Validation은 2024-07~2025-06으로 사용한다.
- 2025-07~2026-07은 `post-selection temporal evaluation`이며 새로운 독립 Final Test라고 부르지 않는다.
- Walk-forward는 2023년 전체를 최초 학습기간으로 사용하고 2024-01부터 월별 expanding fold로 진단한다. 분기는 월별 결과의 보조 rollup으로만 사용한다.
- Validation 선택 범위를 전처리·결측·범주 처리·Logistic 수렴·calibration·REVIEW_REQUIRED 2개 및 사전 정의한 소수 설정으로 제한한다.

이유:

- 권고안은 Train 2,675경주, Validation 1,759경주, 이후 평가 1,923경주를 확보하고 각 Train·Validation·평가가 12개 월-of-year를 포함한다.
- 기준안의 6개월 Validation은 규모는 충분하지만 1~6월만 포함하며, 2025년 전체를 Validation으로 쓰는 안은 평가기간이 7개월로 줄어든다.
- 월별 fold도 최소 108경주·1,230행을 확보해 변화 시점을 유지할 수 있으며 분기 fold보다 진단 해상도가 높다.

## 2026-08-13 — Race-time median 제외와 고정 길이 sectional 유지

결정:

- `horse_recent3_race_time_median`, `horse_recent5_race_time_median`은 Snapshot에 유지하되 official
  baseline v2 모델 입력에서 `EXCLUDE_LOGICAL`로 확정 제외한다.
- Validation에서 두 Feature의 포함 여부를 다시 선택하지 않는다.
- race-time count와 S1F/G3F/G1F recent3/5 median·count는 현행 정의 그대로 유지한다.
- official 입력 계약은 117개이며 `REVIEW_REQUIRED`는 0개다.

이유:

- 절대 경주시간은 거리·경마장을 섞으면 물리적으로 같은 성능량이 아니지만, count는 관측 깊이만 나타낸다.
- S1F·G3F·G1F는 각각 첫 200m·마지막 600m·마지막 200m의 고정 구간으로 경마장별 원천을
  초 단위로 정규화했다. 부산경남 누적값과 직접값의 전체기간 관계도 오차 없이 성립했다.
- 조건별 분포 차이는 존재하지만 구간 정의를 훼손하지 않으므로 성능을 보지 않은 상태에서
  불필요한 경마장 분리나 거리 조건 Feature를 추가하지 않는다.
